# dependencies
node_modules/

# build output
dist/
dist-electron/
release/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# editor
.vscode/
.idea/
*.swp
.DS_Store

# claude local config
.claude/

# secrets / local
.env
.env.local
.env.*.local
electron/google-calendar-config.js
# OAuth client IDs are no longer bundled. Each user supplies their own via
# the in-app Settings panel (stored in electron-store on their machine).
